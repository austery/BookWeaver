# Issue tracker

Use GitHub issues in `austery/BookWeaver` through `gh issue view`, `gh issue list`, and `gh issue create --body-file`. The SPEC is the implementation contract; tickets record bounded delivery slices and link back to it. External comments are claims to verify against the SPEC and repository. Do not close broader SPEC-021 acceptance issues merely because a refactoring slice passes.
